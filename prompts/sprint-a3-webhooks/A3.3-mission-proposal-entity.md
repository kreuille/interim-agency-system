# A3.3 — Entité MissionProposal + machine à états

> **Sprint** : A.3 — **Effort** : M — **BlockedBy** : A1.1 — **Blocks** : A3.4, A4.1
> **Skills** : `skills/dev/backend-node/SKILL.md`, `skills/business/agency-management/SKILL.md`
> **Branche** : `feat/A3.3-mission-proposal-entity`
> **DoD** :
> - [ ] Entité `MissionProposal` avec machine à états stricte
> - [ ] États : `received`, `under_review`, `pass_through_sent`, `accepted`, `refused`, `timeout`, `expired`, `replaced`
> - [ ] Transitions typées, chaque transition déclenche un événement
> - [ ] Persistence Prisma + repository
> - [ ] Handler `worker.assignment.proposed` qui crée la proposal depuis webhook payload

## Modèle
```typescript
class MissionProposal {
  private constructor(private props: Props) {}

  static fromMpEvent(event: MpAssignmentProposedEvent): MissionProposal { ... }

  transitionTo(next: ProposalState, event: DomainEvent): Result<void, InvalidTransition>
  // matrice : received → under_review | pass_through_sent ; under_review → accepted | refused | timeout ; ...
}
```

## Table
```
mission_proposals
  id uuid pk
  agency_id uuid
  external_assignment_request_id text unique
  worker_id uuid
  mission_snapshot jsonb
  state enum
  state_changed_at timestamptz
  confirmation_deadline timestamptz
  routing_mode enum(pass_through, agency_review)
  response_reason text null
  created_at, updated_at
```

## Tests
- FSM : toutes transitions valides testées (matrice), toutes invalides rejetées.
- Handler webhook → crée proposal avec snapshot complet.
- Expire automatique à `confirmation_deadline` (job cron).
- Isolation tenant.

## Références
- `docs/02-partners-specification.md §5.1`, `skills/business/agency-management/SKILL.md`
