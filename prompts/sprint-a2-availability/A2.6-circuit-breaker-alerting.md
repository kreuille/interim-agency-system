# A2.6 — Circuit breaker opossum + alerting Sentry

> **Sprint** : A.2 — **Effort** : S — **BlockedBy** : A2.4 — **Blocks** : —
> **Skills** : `skills/dev/devops-swiss/SKILL.md`
> **Branche** : `feat/A2.6-circuit-breaker-alerting`
> **DoD** :
> - [ ] Opossum wrapper autour de **tous** les appels MP, config standardisée
> - [ ] Ouverture si 50% erreurs sur fenêtre 10 appels / 30s, half-open après 30s
> - [ ] Événement Sentry sur ouverture + fermeture du breaker
> - [ ] Métrique Prometheus `mp_cb_state{endpoint}` exposée
> - [ ] Runbook `docs/runbooks/mp-unreachable.md` rédigé
> - [ ] Dashboard Grafana "MovePlanner health"

## Fichiers
- `apps/api/src/infrastructure/reliability/cb.factory.ts`
- `apps/api/src/infrastructure/observability/sentry.ts`
- `docs/runbooks/mp-unreachable.md`

## Config opossum standard
```typescript
new CircuitBreaker(fn, {
  timeout: 10_000,
  errorThresholdPercentage: 50,
  resetTimeout: 30_000,
  rollingCountTimeout: 30_000,
  rollingCountBuckets: 10,
  name: 'mp-availability-push',
})
```

## Runbook `mp-unreachable.md`
- Déclencheur : `mp_cb_state{endpoint="..."}=open` > 10 min
- Actions : vérifier status page MP, contacter contact MP, bascule des jobs critiques (availability push) en "paused" via flag, communiquer interne, éviter de surcharger par retry.

## Références
- `skills/dev/devops-swiss/SKILL.md`, `skills/integration/moveplanner-api/SKILL.md`
