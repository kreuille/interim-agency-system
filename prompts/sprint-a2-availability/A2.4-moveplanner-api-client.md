# A2.4 — Client API MovePlanner typé (mTLS + rotation clé 90j)

> **Sprint** : A.2 — **Effort** : L — **BlockedBy** : A0.6 — **Blocks** : A2.5, A3.4, A4.7
> **Skills** : `skills/integration/moveplanner-api/SKILL.md`, `skills/dev/security-hardening/SKILL.md`, `skills/dev/api-rest-design/SKILL.md`
> **Branche** : `feat/A2.4-moveplanner-api-client`
> **DoD** :
> - [ ] Client HTTP typé généré depuis OpenAPI MP (via `openapi-typescript` ou `orval`)
> - [ ] mTLS configuré (cert+key stockés en Secret Manager, reload à chaud)
> - [ ] Auth Bearer + rotation clé API 90j (grace 7j 2 clés acceptées)
> - [ ] Idempotency-Key UUID v4 sur POST/PUT, persisté en `outbound_idempotency_keys`
> - [ ] Circuit breaker opossum, retry backoff exp (1s,5s,15s,60s,300s) uniquement 5xx/timeout/réseau
> - [ ] Métriques Prometheus : `mp_request_total`, `mp_request_duration_seconds`, `mp_cb_state`
> - [ ] Logs OpenTelemetry : span par appel avec attrs endpoint/status/durée

## Endpoints à couvrir
Voir `skills/integration/moveplanner-api/SKILL.md §Endpoints`. Les 10 endpoints.

## Architecture
```
domain/moveplanner/ports/
  worker-push.port.ts
  availability-push.port.ts
  assignment-response.port.ts
  timesheet-sign.port.ts

infrastructure/moveplanner/
  mp-client.ts                # http layer
  adapters/
    worker-push.adapter.ts
    availability-push.adapter.ts
    ...
  idempotency.store.ts        # outbound_idempotency_keys
  cb.factory.ts               # opossum wrappers
  cert-rotation.service.ts
```

## Tests
- Mock MP (A0.2) respond 200 → Result ok.
- Mock MP 500 → retry 5x puis circuit breaker.
- Mock MP 429 → pas de retry immédiat, backoff long.
- Idempotency key rejouée → retourne la réponse cachée (pas 2e appel MP).
- mTLS avec cert expiré → erreur claire avant l'appel.
- Tests de contrat : fixtures OpenAPI MP rejouées.

## Références
- `docs/02-partners-specification.md §7`, `skills/integration/moveplanner-api/SKILL.md`
